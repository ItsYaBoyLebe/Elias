# TODO

- [ ] Add a favicon (show in browser tab + Google search results). Place the file at the root and add `<link rel="icon" href="/favicon.ico" />` (and optionally a 192px PNG for Android) to all HTML files.

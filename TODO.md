# TODO

- [ ] Add a favicon (show in browser tab + Google search results). Place the file at the root and add `<link rel="icon" href="/favicon.ico" />` (and optionally a 192px PNG for Android) to all HTML files.
- [ ] Fill in real social links — Facebook and Instagram are set to `#` in `data/config.js`. Update them once Elias has his profiles set up.
- [ ] Create a dedicated OG image (1200×630px banner) for better social sharing previews on WhatsApp/Facebook. Save as `assets/img/og-image.jpg`, run `npm run optimise-images`, and update the `og:image` + `twitter:image` meta tags in all HTML files.
